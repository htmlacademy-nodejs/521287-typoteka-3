extends ../layout.pug

block content
  main.main-page
    h1.visually-hidden Главная страница личного блога Типотека
    p.
      Это приветственный текст, который владелец блога может выбрать, чтобы описать себя 👏

    include ../partials/categories.pug

    .main-page__section-flex
      include ../partials/popular-articles.pug
      include ../partials/last-comments.pug
    
    if (articles)
      section.main-page__list.preview
        h2.visually-hidden Список превью статей
        ul.preview__list
          each article in articles
            li(class={'preview__item': true, 'preview__item--noimage': !article.picture})
              include ../partials/article-card.pug
      
      .preview__toggle-wrapper
        +pagination(page, totalPages, 2, `?`, 'preview__pagination')
